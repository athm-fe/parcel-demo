{
  "name": "parcel-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/*.html",
    "build": "parcel build src/*.html --out-dir build/ --public-url ./ --no-minify --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "jpuncle",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.8.1"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "normalize.css": "^7.0.0"
  }
}
