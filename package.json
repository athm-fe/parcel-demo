{
  "name": "parcel-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run clean",
    "start": "parcel src/*.html --open",
    "prebuild": "npm run clean",
    "build": "parcel build src/*.html --out-dir build/ --public-url ./ --no-cache --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./.cache ./dist ./build"
  },
  "keywords": [],
  "author": "jpuncle",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^9.0.2",
    "node-sass": "^4.9.2",
    "parcel-bundler": "^1.9.7"
  },
  "dependencies": {
    "lodash": "^4.17.10",
    "normalize.css": "^8.0.0"
  }
}
